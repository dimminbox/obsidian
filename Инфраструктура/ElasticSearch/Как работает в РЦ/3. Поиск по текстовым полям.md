Получает id документов, у которых релевантное название, потом эти id подставляются в финальный запрос как одна из частей dis_max.

{
  "query": {
    "bool": {
      "must": {
        "dis_max": {
          "tie_breaker": 1,
          "queries": [{
            "function_score": {
              "query": { "match": { "SEARCH.NAME.TITLE": { "query": "ручка", "analyzer": "NAME_TITLE_ANALYZER" } } },
              "functions": [{
                "script_score": {
                  "script": {
                    "lang": "painless",
                    "source": "double score = 0;\nint count = 0;\nint size = 0;\ndef original = doc[params.field];\ndef fieldSize = original.size();\n\nfor (item in params.tokens) {\n     size++;\n     if (original != null) {\n         int position = original.indexOf(item.token); \n         if (position != -1) { \n             count ++; \n             if (params.config.existence == true) {\n                 score = score + params.factor / params.tokens.length\n             }\n         } \n     } \n}\n\nif (params.config.full == true) {\n     if (size == count) {\n          score = score + params.factor;\n     }\n} \n\nif (params.config.total == true) { \n     if (fieldSize == count) { \n         score = score + params.factor; \n     }\n} \n\nif (params.config.all == true) { \n    if ((size == count) && (fieldSize == count)){\n     score = score + params.factor;\n     }\n} \n\nreturn score;",
                    "params": { "field": "SEARCH.NAME.TITLE", "tokens": [{"token": "ручка"}], "config": {"existence": false, "total": true, "all": false, "full": false}, "factor": 5000 }
                  }
                }
              }]
            }
          }]
        }
      }
    }
  },
  "min_score": 50,
  "_source": ["ID", "SEARCH.TITLE"],
  "size": 9000
}