Это преобразование функции в цепочку функций снесколькими аргументами, каждая из которых принимает только один аргумент. 

`Func<int, Func<int, Func<int, int>>> Add = a => b => c => a + b + c; 
var add3 = Add(3); // запомнили a = 3 
var add3_5 = add3(5); // запомнили b = 5 
add3_5(5);`